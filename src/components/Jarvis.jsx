import React from 'react';
import jarvis from '../assets/jarvis.gif';
const Jarvis = () => {
  return(
  <div className='Jarvis' id='jarvis' style={{ background: `url(${jarvis})`, }}>

  </div>);
};

export default Jarvis;
